elasticsearch:
  namespace: gnida-dev
  replicas: 3

  tls:
    enabled: false # Полностью отключаем TLS
    selfSignedCertificate: false # Отключаем создание самоподписанных сертификатов

  nodeSets:
    - name: default
      count: 3
      config:
        node.store.allow_mmap: false
        xpack.security.enabled: false # Отключаем безопасность
        discovery.type: single-node
      volumeClaimTemplates:
        - metadata:
            name: elasticsearch-data
          spec:
            accessModes:
              - ReadWriteOnce
            resources:
              requests:
                storage: 10Gi
            storageClassName: "standard"

  elasticsearch:
    extraEnvs:
      - name: xpack.security.enabled
        value: "false"
      - name: xpack.security.transport.ssl.enabled
        value: "false"
      - name: xpack.security.http.ssl.enabled
        value: "false"
    podTemplate:
      spec:
        containers:
          - name: elasticsearch
            volumeMounts: [] # Убираем все монтирования
        volumes: [] # Убираем все volumes

  # Удаляем секреты сертификатов
  secretMounts: []

  resources:
    limits:
      memory: 2Gi
      cpu: 1000m
    requests:
      memory: 1Gi
      cpu: 500m

kibana:
  namespace: gnida-dev
  replicas: 1

  elasticsearchHosts: http://elasticsearch-master.gnida-dev.svc.cluster.local:9200 # HTTP вместо HTTPS

  resources:
    limits:
      memory: 1Gi
      cpu: 500m
    requests:
      memory: 512Mi
      cpu: 250m
